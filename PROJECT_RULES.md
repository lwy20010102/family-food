# 项目开发规则

## 1. 修改前先阅读

在修改代码之前：

1. 阅读相关文件；
2. 理解当前代码逻辑；
3. 不要根据猜测修改；
4. 不要重复创建已有功能。

---

## 2. 最小修改原则

修改功能时：

- 优先修改必要文件；
- 不要重构无关代码；
- 不要删除正常工作的功能；
- 不要擅自更换技术框架。

---

## 3. 每次只完成一个任务

不要一次性：

- 修改前端；
- 修改后端；
- 重构数据库；
- 添加多个新功能。

必须围绕当前任务完成最小闭环。

---

## 4. 修改后检查

每次修改后：

1. 检查语法；
2. 检查类型；
3. 检查导入；
4. 检查 API；
5. 尽可能运行测试。

---

## 5. 数据库修改

修改数据库模型时：

必须同时检查：

- Model；
- Schema；
- Migration；
- API；
- 前端类型。

不要只修改其中一个。

---

## 6. API 修改

修改 API 时：

必须检查：

- 后端路由；
- 请求参数；
- 返回数据；
- 前端调用；
- TypeScript 类型。

---

## 7. AI 功能

所有 AI 调用：

- API Key 必须来自环境变量；
- 不允许写死；
- AI 调用逻辑必须放在 Service；
- AI 返回数据必须验证；
- AI 返回异常必须处理；
- AI 生成结果必须允许用户修改。

---

## 8. 禁止事项

禁止：

- 删除 `.env.example`；
- 提交 API Key；
- 提交真实密码；
- 大规模重构正常工作的代码；
- 为了“优化”而修改无关代码；
- 未经确认添加复杂基础设施。

---

## 9. 遇到不确定需求

如果需求存在两种完全不同的实现方式：

先说明：

方案 A

方案 B

推荐方案

然后等待确认。

不要擅自选择会大幅影响项目结构的方案。

---

## 10. 完成任务后的输出格式

每次完成任务后必须说明：

### 完成内容

-

### 新增文件

-

### 修改文件

-

### 如何测试

-

### 已知问题

-

### 下一步

-